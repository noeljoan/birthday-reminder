"""Application entry point for Birthday Reminder.

Running this script launches the CustomTkinter GUI.
"""

# Import the GUI entry point. Since this file is executed directly (not as a
# package), we use a plain absolute import.
import gui

if __name__ == "__main__":
    gui.main()
